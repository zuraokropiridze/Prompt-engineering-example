<!DOCTYPE html>
<html lang="ka">
<head>
    <meta charset="UTF-8">
    <title>Prompt Engineering Guide</title>
    <link rel="stylesheet" href="test.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>

    <div class="guide-container">
        <header>
            <h1>Prompt <span class="accent">Engineering</span> 101</h1>
            <p>ისწავლე AI-სთან ეფექტური კომუნიკაცია</p>
        </header>

        <section class="formula-section">
            <h2>ფორმულა: <span class="code-text">Persona + Context + Task + Constraint</span></h2>
            
            <div class="grid">
                <div class="card">
                    <i class="fas fa-user-ninja"></i>
                    <h3>Persona (როლი)</h3>
                    <p>"იმოქმედე როგორც უფროსი დეველოპერი..."</p>
                </div>

                <div class="card">
                    <i class="fas fa-database"></i>
                    <h3>Context (კონტექსტი)</h3>
                    <p>"მე ვწერ აპლიკაციას სტუდენტებისთვის..."</p>
                </div>

                <div class="card">
                    <i class="fas fa-tasks"></i>
                    <h3>Task (დავალება)</h3>
                    <p>"დამიწერე ფუნქცია, რომელიც..."</p>
                </div>

                <div class="card">
                    <i class="fas fa-exclamation-triangle"></i>
                    <h3>Constraint (შეზღუდვა)</h3>
                    <p>"არ გამოიყენო გარე ბიბლიოთეკები..."</p>
                </div>
            </div>
        </section>

        <section class="example-box">
            <h3>იდეალური პრომპტის მაგალითი:</h3>
            <div class="prompt-text">
                <span class="p-role">Role:</span> Expert Python Coder<br>
                <span class="p-context">Context:</span> Data cleaning for a CSV file<br>
                <span class="p-task">Task:</span> Create a function to remove null values<br>
                <span class="p-format">Format:</span> Clean code with comments
            </div>
            <button class="copy-btn">დააკოპირე პრომპტი</button>
        </section>
    </div>

</body>
</html>

:root {
    --bg: #121212;
    --surface: #1e1e1e;
    --accent: #9d4edd;
    --accent-dark: #7b2cbf;
    --text: #e0e0e0;
}

body {
    background-color: var(--bg);
    color: var(--text);
    font-family: 'Inter', sans-serif;
    display: flex;
    justify-content: center;
    padding: 40px 20px;
    margin: 0;
}

.guide-container {
    max-width: 900px;
    width: 100%;
}

header {
    text-align: center;
    margin-bottom: 50px;
}

.accent { color: var(--accent); }

.formula-section h2 {
    text-align: center;
    background: #252525;
    padding: 15px;
    border-radius: 12px;
    border-left: 5px solid var(--accent);
}

.grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 20px;
    margin-top: 30px;
}

.card {
    background: var(--surface);
    padding: 25px;
    border-radius: 15px;
    text-align: center;
    transition: 0.3s;
    border: 1px solid #333;
}

.card i {
    font-size: 30px;
    color: var(--accent);
    margin-bottom: 15px;
}

.card:hover {
    transform: translateY(-10px);
    border-color: var(--accent);
    box-shadow: 0 10px 20px rgba(157, 78, 221, 0.2);
}

.example-box {
    margin-top: 50px;
    background: #1a1a1a;
    padding: 30px;
    border-radius: 20px;
    border: 1px dashed var(--accent);
}

.prompt-text {
    background: #000;
    padding: 20px;
    border-radius: 10px;
    font-family: 'Courier New', monospace;
    line-height: 1.6;
    margin: 20px 0;
}

.p-role { color: #ff79c6; font-weight: bold; }
.p-context { color: #8be9fd; }
.p-task { color: #50fa7b; }
.p-format { color: #f1fa8c; }

.copy-btn {
    background: var(--accent);
    color: white;
    border: none;
    padding: 12px 25px;
    border-radius: 8px;
    cursor: pointer;
    font-weight: bold;
    transition: 0.3s;
}

.copy-btn:hover { background: var(--accent-dark); }
.copy-btn:active { transform: scale(0.95); }
